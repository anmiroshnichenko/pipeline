node('linux') {
    stage('Get a project') {
        checkout scmGit(
            branches: [[name: 'main']],
            userRemoteConfigs: [[url: 'https://github.com/anmiroshnichenko/DZ_8.2']]
        )
    }
    // stage('test') {
    //     sh /usr/local/go/bin/go test .
    // }
}